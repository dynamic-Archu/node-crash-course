const people = ['yoshi','yu','archu','viffy'];
const ages = [25,30,20,20];

console.log(people)
// creating a property of the people here ,if people property name is same as people const 
// doesnt want to explicitly mention it

module.exports = {
    people, ages
};

// the object xyz incorporates these 2 values people and ages 